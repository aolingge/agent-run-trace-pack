# Backlog

## Next

- [ ] Add a GitHub Actions wrapper example that uploads the generated trace pack as a workflow artifact.
- [ ] Add a sample CI transcript fixture that shows how maintainers can review a redacted failure report.
- [ ] Add a docs page that compares when to use `run`, `summarize`, `doctor`, and `init` in a contributor workflow.
